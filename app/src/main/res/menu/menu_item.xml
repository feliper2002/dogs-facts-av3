<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/factListMenu"
        android:title="@string/save_facts"
        android:icon="@drawable/ic_list"
        app:showAsAction="always"
        android:tooltipText="@string/save_facts"
        tools:ignore="AlwaysShowAction"
        />
    <item
        android:id="@+id/appConfig"
        android:title="@string/app_config"
        app:showAsAction="never"
        android:tooltipText="@string/app_config"
        />
    <item
        android:id="@+id/appInfo"
        android:title="@string/app_info"
        app:showAsAction="never"
        android:tooltipText="@string/app_info"
        />

</menu>